# tretton37 task description

Backend.

## Technologies

* `NodeJS`
* `ExpressJS`
* `Typescript`
* `Jest`

## How to run

`yarn start` at local machine -> project runs on port 9000 (http://localhost:9000)
`yarn test` to run tests
